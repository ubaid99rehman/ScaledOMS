<UserControl x:Class="OMS.Orders.OrderHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" 
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" 
             xmlns:Internal="clr-namespace:DevExpress.Xpf.Core.Internal;assembly=DevExpress.Xpf.Core.v24.1"              
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="OrderHsy" Grid.Row="0" Margin="10 10 10 10">
            <TextBlock.Effect>
                <Internal:BadgeShaderEffect/>
            </TextBlock.Effect><Run Text="Orders History"/></TextBlock>

        <Grid Grid.Row="1">
            <!--Working-->
            <dxg:TreeListControl ItemsSource="{Binding Orders}">

                <dxg:TreeListControl.View>
                    <dxg:TreeListView KeyFieldName="ID"
                              ParentFieldName="ParentID" 
                              AllowEditing="False" 
                              ShowIndicator="False" AutoWidth="True" AutoExpandAllNodes="True"/>
                </dxg:TreeListControl.View>

                <dxg:TreeListControl.Columns>
                    <dxg:TreeListColumn FieldName="ID" Header="ID" Visible="False" />
                    <dxg:TreeListColumn FieldName="Status" Header="Order Status" />
                    <dxg:TreeListColumn FieldName="OrderID" Header="Order ID" />
                    <dxg:TreeListColumn FieldName="OrderDate" Header="Order Date" />
                    <dxg:TreeListColumn FieldName="Symbol" Header="Symbol" />
                    <dxg:TreeListColumn FieldName="OrderType" Header="Order Type" />
                    <dxg:TreeListColumn FieldName="AccountID" Header="Account" />
                    <dxg:TreeListColumn FieldName="Price" Header="Price" />
                    <dxg:TreeListColumn FieldName="Quantity" Header="Quantity" />
                    <dxg:TreeListColumn FieldName="Total" Header="Total" />
                    <dxg:TreeListColumn FieldName="ExpirationDate" Header="Expiration Date" />
                    <dxg:TreeListColumn FieldName="LasUpdatedDate" Header="Last Updated" />
                </dxg:TreeListControl.Columns>
            </dxg:TreeListControl>
        </Grid>

    </Grid>
</UserControl>
